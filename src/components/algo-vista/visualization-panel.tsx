// This global component is deprecated.
// Visualizers now use their own local versions (e.g., src/app/visualizers/[slug]/VisualizationPanel.tsx).
// This file can be safely deleted if no longer referenced.
// Exporting null to prevent build errors if accidentally imported.
export default () => null;
